<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<title>MY APP</title>
	</head>
	<body>

		<ul class="quotes">
			<% for(var i = 0; i < quotes.length; i++) { %>
			<li class="quote">
				<span class="name"><b><%= quotes[i].name %></b></span>
				<span class="content"><%= quotes[i].quote %></span>
				<a href="#" class="update">Update</a>
				<a href="<%= quotes[i]._id %>" class="delete">Delete</a>
			</li>
			<% } %>
		</ul>


		<form action="/quotes" method="POST">
			<input type="text" placeholder="name" name="name" />
			<input type="text" placeholder="quote" name="quote" />
			<button type="submit">Submit</button>
	</form>
	
	<script src="main.js"></script>	
	
	</body>
</html>
